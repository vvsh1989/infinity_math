# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요

**수학 뽁뽁이 (Math Pop)** — 뇌로 돌리는 피젯 토이. 한국 중학교 1학년 수준의 쉬운 수학 문제를 끝없이 풀 수 있는 가벼운 웹 앱.

학습 앱이 아니다. 버블랩, 피젯 스피너 같은 "뇌 간식" 경험이 목표. 시험 대비, 강의 플랫폼, 경쟁 퀴즈 앱처럼 만들지 말 것.

핵심 흐름: 페이지 열기 → 문제 즉시 표시 → 4지선다 탭 → 즉시 채점 피드백 → 500~900ms 후 자동으로 다음 문제. "정답 확인" 버튼 없음 — 답 탭이 곧 확인.

## 기술 스택과 구조

- 순수 HTML/CSS/JS 정적 구현 (또는 최소한의 Vite). 백엔드 없음, 로그인 없음.
- 데이터 저장은 `localStorage`만 사용.

권장 파일 구조:

```text
/index.html
/styles.css
/src/main.js
/src/problemGenerators.js   # 카테고리별 문제 생성기
/src/storage.js             # localStorage 입출력
/src/utils.js               # randomInt, shuffle, makeChoices 등
/src/slingshot.js           # 새총 피젯 (퀴즈 흐름과 완전 독립)
/scripts/                   # Node 검증 스크립트 (앱에 포함 안 됨)
```

## 새총 피젯 규칙

지켜야 할 법칙은 단 하나: **고무줄로 당긴 만큼, 반대 방향으로 튕겨 나간다.**

- 날아가는 물건은 아무거나 (작은 이모지 — 자동차, 냉장고, 동전 등). 정해진 돌멩이 같은 것 없음.
- 비행 물리는 정확하지 않아도 됨. 제멋대로 날아도 됨 (무중력, 역중력, 비틀거림 전부 허용).
- 벽에서 튕기지 않음 — 화면 밖으로 그냥 날아가고, 사라질 때 비거리(m)를 표시.
- 비거리는 발사 속도와 대체로 일치해야 함: 속도²에 비례하고 랜덤은 ±20% 양념 수준만. 살짝 당겼는데 수백만 m가 나오면 안 됨.
- 예외 하나: 극악의 확률(0.1%)로 "4,444배 돌풍" 잭팟이 터져 비거리가 4,444배가 됨. 이때도 기반값은 속도 비례.
- 비거리 1~3위 순위판(🥇🥈🥉)을 localStorage에 저장. 메인의 "기록 초기화" 버튼이 `stats-reset` 커스텀 이벤트를 쏘면 순위도 함께 초기화됨.
- 퀴즈 풀이 리듬을 막으면 안 됨 — 날아다니는 물건은 `pointer-events: none` 레이어 위에 있고, 새총은 카드 아래 독립 영역.
- 동시 비행 개수 제한(MAX_ACTIVE)으로 성능 보호. 비행체가 없으면 rAF 루프 정지.

## 문제 생성기 규칙

각 생성기는 독립적으로 테스트 가능한 순수 함수이며, 다음 구조의 객체를 반환:

```js
{
  category: "linear-equation",
  question: "3x + 5 = 20, x는?",
  choices: ["3", "4", "5", "6"],  // 정확히 4개, 중복 없음, 셔플됨
  answer: "5",                     // choices에 반드시 포함
  explanation: "3x + 5 = 20 → 3x = 15 → x = 5"  // 짧게
}
```

- 오답 보기 생성 시 무한 루프 주의 (중복 회피 로직).
- 헬퍼: `randomInt(min, max)`, `shuffle(array)`, `makeChoices(correctAnswer, wrongAnswers)`.

문제 카테고리 (중1 수준): 정수 사칙연산(음수 포함), 약수/배수/최대공약수/최소공배수, 소인수분해(작은 수만), 동류항 정리(`2a + 3a`), 일차방정식(`ax + b = c`, 답은 대부분 정수), 좌표(사분면 판별), 비례식(`2 : 3 = 8 : ?`), 평균(작은 수 3~5개).

난이도 규칙: 3~10초 안에 암산으로 풀 수 있어야 함. 긴 서술형 문제 금지 ("철수는 사과를..." ❌, `-7 + 12 = ?` ⭕). 고등 수학, 이차방정식, 기하 도형, 복잡한 분수, 함정 문제 금지.

## localStorage 스키마

```js
{
  totalSolved: number,
  totalCorrect: number,
  totalWrong: number,
  currentStreak: number,
  bestStreak: number,
  lastPlayedDate: string
}
```

## 상호작용 규칙

답 선택 시: ① 모든 버튼 즉시 비활성화 → ② 정답/오답 표시 → ③ 선택한 답 하이라이트 → ④ 틀렸으면 정답도 하이라이트 → ⑤ 통계 갱신 → ⑥ 500~900ms 후 자동으로 다음 문제.

오답 시 currentStreak는 0으로 리셋, bestStreak는 유지.

## 피드백 톤

가볍고 장난스럽게, 절대 면박 주지 않기. 사용자 노출 텍스트는 전부 한국어.

- 정답 예: "뽁. 정답.", "뇌가 방금 기지개 켰음."
- 오답 예: "삐끗. 정답은 5.", "괜찮음. 뇌가 잠깐 창문 봄."
- 금지: "틀렸습니다. 공부하세요.", "실패!"

말투는 캐주얼하고 짧게 — 유치하거나 강의식이면 안 됨.

## UI 방향

모바일 퍼스트. 화면 구성: 앱 타이틀 / 작은 통계 행 / 문제 카드 / 답 버튼 4개 / 피드백 영역 / (선택) 다음 버튼 / 작은 통계 리셋 버튼.

큰 문제 텍스트, 탭하기 좋은 큰 버튼, 부드러운 라운드 카드, 차분한 색. 애니메이션은 절제: 정답 시 작은 pop, 오답 시 살짝 shake/fade. 과한 게임 이펙트 금지.

접근성: 시맨틱 `<button>`, 키보드 접근 가능, 보이는 포커스 상태, 정답/오답을 색에만 의존해 표시하지 말 것.

## 검증 체크리스트

자동화 테스트가 없으므로 작업 완료 전 수동 확인하고, 어떤 수동 검증을 했는지 명시할 것:

- 페이지가 에러 없이 로드되고 문제가 즉시 표시됨
- 버튼 4개 모두 동작, 보기 중복 없음, 정답이 항상 보기에 포함
- 정답 시 streak 증가, 오답 시 currentStreak 리셋, bestStreak 저장
- 새로고침 후 통계 유지 (localStorage)
- 다음 문제 자동 전환
- 모바일 레이아웃 정상, 콘솔 에러 없음

## MVP에서 만들지 말 것

명시적 요청 없이는 구현 금지: 계정/백엔드/DB, 랭킹, 결제, 광고, AI 문제 생성, 이미지 기반 기하, 분석 도구, 소셜 기능, 푸시 알림, 앱 스토어 패키징.

## 우선순위

충돌 시 이 순서로 판단: ① 풀이 리듬 빠르게 → ② 문제 짧게 → ③ UI 깔끔하게 → ④ 코드 유지보수 가능하게 → ⑤ 불필요한 기능 배제. 작은 순수 함수, 명확한 이름, 과한 추상화 금지.
